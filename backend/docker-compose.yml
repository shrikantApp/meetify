version: "3.9"

services:

  api:
    build: .
    container_name: meetify_api
    restart: always
    ports:
      - "${PORT}:4001"
    env_file:
      - .env
    depends_on:
      - db
    networks:
      - meetify_network

  # db:
  #   image: postgres:16
  #   container_name: meetify_postgres
  #   restart: always
  #   ports:
  #     - "5432:5432"
  #   env_file:
  #     - .env
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - meetify_network

volumes:
  postgres_data:

networks:
  meetify_network:
